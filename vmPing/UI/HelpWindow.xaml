<Window x:Class="vmPing.UI.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="vmPing 帮助"
        UseLayoutRounding="True"
        Height="575"
        Width="750"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style TargetType="Paragraph">
            <Setter Property="TextAlignment" Value="Left" />
            <Setter Property="FontFamily" Value="Segoe UI, Arial" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <!-- Title style -->
        <Style x:Key="TitleText"
               BasedOn="{StaticResource {x:Type TextBlock}}"
               TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="FontSize" Value="26" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <!-- HyperLink style -->
        <Style x:Key="HyperlinkStyle"
               BasedOn="{StaticResource {x:Type Hyperlink}}"
               TargetType="Hyperlink">
            <Setter Property="TextDecorations" Value="None" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="#6c71c4" />
        </Style>

        <!-- Version style -->
        <Style x:Key="VersionText"
               BasedOn="{StaticResource {x:Type Paragraph}}"
               TargetType="Paragraph">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="#707070" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <!-- Section header style -->
        <Style x:Key="HeaderText"
               BasedOn="{StaticResource {x:Type Paragraph}}"
               TargetType="Paragraph">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,40,0,0" />
            <Setter Property="Foreground" Value="#4040c0" />
            <Setter Property="FontSize" Value="26" />
        </Style>

        <!-- Sub-header style -->
        <Style x:Key="SubHeaderText"
               BasedOn="{StaticResource {x:Type Paragraph}}"
               TargetType="Paragraph">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,25,0,0" />
            <Setter Property="Foreground" Value="#404040" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <!-- Paragraph style -->
        <Style x:Key="ParagraphText"
               BasedOn="{StaticResource {x:Type Paragraph}}"
               TargetType="Paragraph">
            <Setter Property="Margin" Value="0,2,0,0" />
            <Setter Property="Foreground" Value="#606060" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TreeView Name="tree" FontSize="10pt" BorderThickness="0" Padding="0,20,20,20" Margin="0">
            <TreeViewItem Header="简介"
                          Selected="Intro_Selected" />
            <TreeViewItem Header="基本用法"
                          Selected="BasicUsage_Selected" />
            <TreeViewItem Header="扩展功能"
                          Selected="ExtraFeatures_Selected" />
            <TreeViewItem Header="选项"
                          Selected="Options_Selected" />
            <TreeViewItem Header="命令行用法"
                          Selected="CommandLineUsage_Selected" />
        </TreeView>
        <GridSplitter Grid.Column="1" Width="4"
                      HorizontalAlignment="Center"
                      BorderBrush="Black"
                      BorderThickness="0"
                      VerticalAlignment="Stretch" />

        <FlowDocumentScrollViewer Name="MainDocument" Grid.Column="2" FocusVisualStyle="{x:Null}">
            <FlowDocument>
                <!-- Logo and title -->
                <BlockUIContainer>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                        <Image Source="/vmPing.ico" Width="32" Height="32" Margin="0,0,12,0" />
                        <TextBlock Style="{StaticResource TitleText}"
                                   VerticalAlignment="Center"
                                   Text="vmPing">
                            <TextBlock.Effect>
                                <DropShadowEffect ShadowDepth="4" Direction="330" Color="Black" Opacity="0.15" BlurRadius="4" />
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </BlockUIContainer>

                <!-- Link to homepage -->
                <Paragraph TextAlignment="Center">
                    <Hyperlink NavigateUri="https://github.com/R-Smith/vmPing"
                               RequestNavigate="Hyperlink_RequestNavigate"
                               Style="{StaticResource HyperlinkStyle}">
                        https://github.com/R-Smith/vmPing
                    </Hyperlink>
                </Paragraph>

                <!-- Version -->
                <Paragraph Name="Version"
                           Style="{StaticResource  VersionText}"
                           Margin="0,3,0,0">
                    Version X.XXXX.XXXX
                </Paragraph>
                <Paragraph Name="Copyright"
                           Style="{StaticResource VersionText}">
                    版权所有 (C) Ryan Smith
                </Paragraph>

                <!-- Intro section -->
                <BlockUIContainer>
                    <Rectangle Margin="0,30,0,0" HorizontalAlignment="Stretch" Fill="#f0f0f0" Height="1" />
                </BlockUIContainer>
                <Paragraph Name="Intro" Margin="0,5"
                           Foreground="#073642">
                    vmPing 是一款网络工具，可快速监控多个主机的连通性。
                    主机监控器可轻松添加和删除，并随应用程序窗口动态调整大小。
                    颜色编码让您一目了然地查看每个主机的状态。
                </Paragraph>
                <BlockUIContainer>
                    <Rectangle HorizontalAlignment="Stretch" Fill="#f0f0f0" Height="1" />
                </BlockUIContainer>

                <!-- Basic Usage section -->
                <Paragraph Name="BasicUsage"
                           Style="{StaticResource HeaderText}">
                    基本用法
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    应用程序概览
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">

                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    监控主机
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    在文本框中输入主机名或 IP 地址，然后按 Enter 或点击"Ping"。
                    vmPing 将持续 Ping 该主机，直到您点击"停止"。
                    响应将显示在文本框上方的输出区域中。
                    主机正常响应时输出区域变为绿色，无响应时变为红色。
                    如果发生错误，输出区域将变为橙色。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    添加主机监控
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    <Run>点击</Run>
                    <Image Source="{StaticResource icon.add}" Width="14" Height="14" Margin="3,0,0,0"/>
                    <Run FontWeight="SemiBold">添加主机</Run>
                    <Run>从菜单栏，或使用键盘快捷键</Run>
                    <Run FontWeight="SemiBold">Alt-A</Run> .
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    删除主机监控
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    将鼠标悬停在主机监控器上时，右上角将出现 X 图标。
                    点击 X 图标即可删除该实例。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    列数
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    点击菜单栏中的"列数"，然后移动滑块到所需列数。
                    这将设置单行显示的主机监控器数量。
                    将根据需要创建更多行。
                </Paragraph>

                <!-- Extra Features section -->
                <Paragraph Name="ExtraFeatures"
                           Style="{StaticResource HeaderText}">
                    扩展功能
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    端口监控
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    要监控特定 TCP 端口，只需输入主机名或 IP 地址后跟冒号和端口号。
                    vmPing 将持续尝试连接到指定端口并显示连接是否成功。
                    例如 -
                    <Bold>SERVER01:80</Bold> ，将监控 SERVER01 的 80 端口。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    路由追踪
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    路由追踪工具用于测量网络延迟并确定到达目标主机的路径。
                    可通过右上角的菜单图标或按 Ctrl-T 访问。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    泛洪测试
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    泛洪测试工具用于生成大量的 ICMP 数据包。
                    为了限制流量，该工具会等待收到回复后再发送下一个 ICMP 数据包。
                    可通过右上角的菜单图标或按 Ctrl-F 访问。
                </Paragraph>

                <!-- Options section -->
                <Paragraph Name="Options"
                           Style="{StaticResource HeaderText}">
                    选项
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    Ping 间隔
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    每次发送数据包之间的等待时间。可以按秒、分钟或小时指定。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    Ping 超时
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    等待响应的最长时间，单位为秒。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    主机离线/在线时发送邮件
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    每次主机状态发生变化时发送邮件。必须指定邮件服务器、收件人地址和发件人地址。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    保持窗口置顶
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    vmPing 窗口将保持在其他所有应用程序窗口之上。
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    记录所有输出到文件
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphText}">
                    将所有 Ping 响应写入文本文件。文件将命名为 &lt;hostname&gt;.txt 并保存到指定目录。
                    如果文件已存在，新内容将追加到现有文件中。
                </Paragraph>

                <!-- Command Line Usage section -->
                <Paragraph Name="CommandLineUsage"
                           Style="{StaticResource HeaderText}">
                    命令行用法
                </Paragraph>
                <Paragraph Style="{StaticResource SubHeaderText}">
                    用法: vmPing [-i 间隔] [-w 超时] [&lt;目标主机>...]
                </Paragraph>

                <!-- TODO: Convert to table-->
                <!--<DockPanel>
                    <TextBlock Style="{StaticResource ParagraphText}"
                                   FontFamily="Consolas Courier New"
                                   FontSize="14"
                                   Width="90">
                            -i interval
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParagraphText}"
                                   TextWrapping="Wrap">
                            Time, in seconds, to wait between sending each ping.
                    </TextBlock>
                </DockPanel>
                <DockPanel>
                    <TextBlock Style="{StaticResource ParagraphText}"
                                   FontFamily="Consolas Courier New"
                                   FontSize="14"
                                   Width="90">
                            -w timeout
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParagraphText}"
                                   TextWrapping="Wrap">
                            Timeout, in seconds, to wait for each reply.
                    </TextBlock>
                </DockPanel>
                <DockPanel>
                    <TextBlock Style="{StaticResource ParagraphText}"
                                   FontFamily="Consolas Courier New"
                                   FontSize="14"
                                   Width="90">
                            target_host
                    </TextBlock>
                    <TextBlock Style="{StaticResource ParagraphText}">
                            Ping the specified host.  Multiple hostnames may be specified.
                    </TextBlock>
                </DockPanel>-->
            </FlowDocument>
        </FlowDocumentScrollViewer>
    </Grid>
</Window>
